/* a Stack follows the FiFo rule 
(First In First Out) which means that the element added last will be removed first.
*/

let stack = [1,2,3,4,5,6,7,8,9,]
console.log(stack.pop())
console.log(stack.push(10))

console.log(stack)